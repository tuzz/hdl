grammar Pins
  include Vars

  rule pins
    inputs ws outputs {
      def to_hash
        { :inputs => inputs.vars.to_array, :outputs => outputs.vars.to_array }
      end
    }
  end

  rule inputs
    "inputs" ws vars
  end

  rule outputs
    "outputs" ws vars
  end
end
